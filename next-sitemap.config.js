/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: "https://psychology-blogs.vercel.app/", // Replace with your domain
    generateRobotsTxt: true, // Generates robots.txt file
    sitemapSize: 5000, // Optional: splits sitemap if too large
  };
  